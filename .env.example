# 复制成 .env 后填入自己的值：cp .env.example .env
# .env 已被 .gitignore 忽略；本模板不含真实密钥，可以安全提交。
# 后端启动时自动加载（backend/dotenv_lite.py），不需要每次手动 export。
# 临时覆盖：启动前 export 同名变量即可（环境变量优先级高于 .env）。

# 智谱开放平台 API Key。留空/不填则界面上不出现 GLM 模型分组，其他功能不受影响。
# 申请与吊销：https://open.bigmodel.cn/usercenter/apikeys
ZHIPU_API_KEY=

# 可选：覆盖默认生成模型（默认 qwen2.5:7b）
# OLLAMA_MODEL=qwen2.5:3b

# 可选：关闭 M3 层级摘要导航（默认开启；摘要为本地抽取式，不调用云端模型）
# HIERARCHY_ENABLED=0

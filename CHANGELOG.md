# 更新日志

本项目所有值得记录的改动都写在这里。日期为绝对日期（YYYY-MM-DD）。
分类：`新增` / `修复` / `优化` / `文档`。

## [未发布]

### 新增

- **追问终于能接上上一轮了**：以前最终回答的 prompt 里一个字的历史都没有，
  历史只用来补全"拿什么去检索"，所以"再展开讲讲""你刚才说的第二点"这类追问
  必然答偏。现在回答时会带上一段「对话背景」（默认最近 6 轮、总共 1200 字、
  单轮 220 字，可用 `HISTORY_*` 环境变量调整或用 `HISTORY_IN_PROMPT=0` 关掉）。
  背景刻意被压到最低地位——**不能当事实依据、不能被引用、和原文冲突时以原文为准**，
  因为模型一旦拿历史当依据，回答里的 [n] 就不再对应真实片段，本项目最核心的
  引用可核验性会被直接破坏。带了几轮、丢了什么、为什么丢，都会写进过程面板的
  「对话背景」这一步，不是黑箱。

- **长会话不会再"忘掉开头"了（默认关闭，需手动开启）**：逐字历史只带最近 6 轮，
  更早的会被整轮丢掉。新增的滚动会话摘要接住这部分——它只在攒够 4 轮掉出窗口的
  对话时才更新一次，短会话一次模型调用都不多花。摘要**不能替代原文证据**：涉及
  小说事实仍然回到这一轮检索到的片段。三处机械防线：摘要超长硬截断、失败沿用上
  一版（不会突然失忆）、**背景里的引用编号一律抹掉**——编号每轮独立重编，上一轮的
  [1] 和这一轮的 [1] 是两段不同原文，留着它等于诱导模型复用一个指向别处的编号。
  默认关着，因为现有多轮评测集最长只有三四轮、根本触发不到摘要，也就无从证明它
  没引入摘要漂移；开启条件写在 docs/experiments/m36-session-summary.md 里。

- **回答现在会记住"我们在聊哪本书、提到过谁"（M3.6 收官）**：新增结构化会话
  事实——书名和人物。和上面的滚动摘要不是一回事：这里**不调用任何模型**，书名
  来自这一轮真实检索到的证据本身，人物名来自人物关系图里已确认存在的人名反查，
  要么查得到、要么精确匹配到，没有"大概对"的中间态，所以不需要摘要那样的开关，
  默认随对话背景一起生效。跨书切换后会记住"现在聊的是哪本"而不是最早提到的那本；
  人物列表超出上限（默认 8 个）时丢最旧提到的，和逐字历史同一套"牺牲最旧"策略。
  刻意不做"用户已确认结论"——能确定信号强度的确认渠道太窄，关键词猜测又太容易
  把反驳误判成确认，留到有更可靠信号时再补。

- **Agent Lab 动作解析加了埋点（M3.2.1 前置）**：规划器每输出一个动作，都会记下
  它是怎么被解析出来的——裸 JSON 一次成功、剥掉 ``` 围栏才成功、还是靠正则抓第一个
  花括号兜底、或者彻底失败。新脚本 `scripts/agent_parse_stats.py` 把这些聚合成
  一张表。这么做是因为「把 JSON 动作协议换成首行标签协议」这件事的三条理由虽然
  都成立，但**在自家模型上到底多久出一次问题从来没测过**，先用真实数据决定排期，
  不凭"这个设计更好"抢跑。最值得看的不是"失败率"而是"正则兜底率"：那是成功了但
  成功得可疑——抓错对象不会报错，会安安静静地执行一个错误的动作。

- **Agent Lab 的单步工具输出不会再撑爆 prompt**：以前只有「最多跑几步」的限制，
  但一步之内能读多少并没有上限——`read_neighbors` / `get_chapter` 一次就能返回
  一整章，大部头的一章本身就可能把 prompt 挤满。现在读到内容之后还有一道字数闸
  （`AGENT_TOOL_MAX_CHARS`，默认 6000）：`read_neighbors` 无条件保住你点名的那
  一段再向两侧生长，`get_chapter` 从末尾开始丢，被截断的事实会写在过程面板里那
  一步的观察结果上，不会悄悄少给证据。

- **多轮追问有评测集了**：`tests/multiturn_test_set.json`（22 条，覆盖指代 /
  否定 / 时间顺序 / 跨书切换 / 纠正上一轮 / 不该改写）配 `scripts/eval_multiturn.py`。
  它一上来就测出一个比"没有记忆"更严重的存量 bug：改写会把你亲口写出的人名换成
  上文里更显眼的那个（你问"巴特尔能牵它吗"，它拿"陆知微能牵它吗"去检索），
  等于**悄悄回答了另一个问题**。修复后 81.8% → 90.9%，详见
  docs/experiments/m36-multiturn-rewrite.md。

- **可以逐条核实回答里的引用了**：原文出处卡片上多了「核实这条」按钮，点一下
  会让模型判断这句话到底有没有被它引用的原文支持，并给出理由。刻意做成"点了
  才跑"而不是自动标注——实测判定的一致率只有 67.9%，尤其"说没依据"时有一半是
  冤枉的（见 docs/experiments/m35-faithfulness-calibration.md），自动弹提示会
  变成狼来了。所以它只是一个你主动发起的参考意见，判定理由和判定用的模型都会
  一并显示，方便你自己对着原文判断。不点不花钱，也不会让回答变慢。

- **Agent Lab 工具接口正式化为统一 ToolSpec（路线图 M6.1 前置项）**：新增
  `src/tool_spec.py`，五个只读工具（列书架 / 混合检索 / 读相邻片段 / 取章节 /
  带引用回答）的名称、描述、参数与结果 JSON Schema、只读属性、风险等级、
  超时集中到一份不可变注册表管理，80 字摘录版权红线改由 `SourceRef` 类型
  约束直接保证；MCP 实验服务器改为从注册表生成工具注册，消除元数据两处
  手写漂移的隐患。对外工具名与参数保持不变，超范围参数的夹取统一收敛到
  Agent Lab 单点处理（如 `read_neighbors` 的 `radius=0` 现在按文档语义只返回
  中心片段，此前会被夹成 1）

### 优化

- **前端 dev server 默认端口从 5173 改为 45173**：5173 是 Vite 的默认端口，
  本机同时开发多个前端项目（甚至非 Vite 的 Electron 应用）时很容易被占用，
  报错后还要手动找出占用者再决定要不要换端口。换成一个不常见的固定端口，
  减少这种偶发冲突；后端 CORS 白名单同步更新，`README.md`/`CONTRIBUTING.md`
  的启动说明和端口一起改了
- **BM25 关键词检索提速约 5 倍**：大部头（《凡人修仙传》约 2 万片段）实测
  BM25 阶段 P50 227ms → 46ms、P95 602ms → 215ms，端到端检索 P50 682ms → 504ms。
  改为两阶段聚合——SQL 内每个查询词只取词频最高的前 200 个候选
  （`BM25_PER_TERM_LIMIT` 可调）、Python 端融合打分，不再为上万条命中行白算分数；
  检索质量经夜间评测门禁与真实库逐用例对照确认无回退。极端场景可能漏掉
  「多词合计相关但单词词频都低」的片段，调大该上限即可逼近旧行为（见
  `keyword_retrieve` 的说明）
- **修复全新数据库无法初始化索引的问题**：人物关系表的两个索引此前在建表语句
  之前执行，导致临时实验库、夜间 CI 库等从零建库直接失败（M4 引入的回归），
  现已随建表顺序一并修正
- **修复夜间检索评测门禁无法启动的问题**：工程化切换到 uv 后该 workflow 仍在
  安装已删除的 requirements.txt，现已改为与 CI 一致的 `uv sync --frozen`

### 修复

- **一条坏掉的历史记录不再让整段对话都打不开**：出处/思考过程/Agent 步骤这些
  结构化数据的格式会随版本演进，以前只要有一条旧记录格式对不上，整个会话的
  历史就会读取失败。现在只丢掉那一条的结构化附加信息，对话正文照常恢复。
- Docker 部署默认只监听本机（`127.0.0.1`）。之前会发布到所有网卡，而本项目
  所有接口都没有鉴权也没有限流，接在公司/公共 WiFi 或云主机上时，同网段的人
  能读你的书架和聊天记录、并无上限消耗你的模型额度。

### 文档

- **M3.4 重排候选数调优实验报告**：`RERANK_CANDIDATE_MULTIPLIER ∈ {2,3,4,6}` 的
  质量×延迟对照完成——用 trace 实证生产路径（TOP_K=3）下四档全被 `RECALL_K=20`
  兜底成同一个候选池、行为完全一致，确认默认值 3 无需调整；multiplier 实际生效的
  大部头评测路径上质量非单调（更大的候选池反而出现 recall@1 回退）而耗时线性上涨，
  批大小探针证明默认 batch_size 已近最优。详见
  [docs/experiments/m34-rerank-tuning.md](docs/experiments/m34-rerank-tuning.md)，
  同步修正了延迟画像报告中重排候选数的笔误（60 → 20）

## [0.8.0] - 2026-08-23

### 新增

- **M4 人物关系图质量闭环**：
  - 关系边 schema v2（幂等迁移）：新增方向、置信度（0~1）、证据类型
    （明确陈述 / 同段共现）、来源片段定位与审核状态五列，v1 旧边自动回填
    为低置信度共现边；每条边可追溯到原文片段
  - LLM 关系抽取：有生成后端时逐对判断"明确关系陈述还是仅同段出现"，
    输出关系方向与置信度并记录来源片段，解析失败自动降级为共现推断；
    抽取结果随人名一并缓存，重建索引零重复调用。共现推断仍是无后端时的兜底，
    并如实标注低置信度
  - 在线质量门槛：默认只把「明确陈述且置信度 ≥0.7」的边放进问答线索
    （`GRAPH_REQUIRE_EXPLICIT` / `GRAPH_MIN_CONFIDENCE` 可调），共现边留在库里
    供人工审核，不再自动展示给模型——实测消除了"把师父当成伴侣"一类假边
  - 人物关系审核界面：侧栏新面板分页列出待审边（人物对/方向/置信度/证据摘录），
    逐条通过或拒绝；被拒绝的边在所有查询中立即不可见
  - 关系抽取评测脚本 `scripts/eval_graph.py` + 原创语料标注集 16 条：
    共现基线 P=50.0% / R=66.7%，LLM 抽取消灭大部分共现假边但受关键词门控
    召回上限约束；`--model` 可对比不同抽取模型，默认零成本跑基线

## [0.7.0] - 2026-08-23

### 新增

- **MCP 只读 PoC（M6.6 提前项）**：`scripts/mcp_server.py` 经 stdio 暴露四个只读
  工具（list_books/search_novels/read_neighbors/get_chapter），结构化输出含
  schema_version 与 80 字摘录红线；真实客户端端到端验证通过，发现的问题
  （SDK v2 结构化输出需具体类型注解等）已记录用于反哺 M6.1 ToolSpec 设计。

- **系统架构文档**：`docs/system-architecture.md` 用 mermaid 图呈现当前全栈
  分层、问答链路时序、索引流水线、数据模型与工程化设施，附设计决策速查表。
- **工程化第二阶段：依赖审计 + Dependabot + 覆盖率**：
  - CI 接入 `pip-audit`（后端）与 `npm audit --audit-level=high`（前端）；据此修复
    全部已知漏洞——后端 fastapi/uvicorn/sentence-transformers 等大版本升级
    （starlette 1.6 / transformers 5.15，219 测试与 embedding 实测兼容）、
    前端 vite 8 / vitest 4 / playwright 1.62，`npm audit` 清零
  - `.github/dependabot.yml`：pip / npm / github-actions 三生态周更，
    minor/patch 分组合并减少 PR 噪音
  - 覆盖率观测：pytest-cov + vitest `test:coverage` 进 CI（只建基线不设硬门槛）
- **M5 第一项：Docker Compose 一键部署**：
  - 多阶段 Dockerfile（Node 构建前端 → uv 装锁定运行时依赖），镜像内
    FastAPI 检测到 `frontend/dist` 自动托管静态文件，单端口对外
  - compose 带 pgvector 数据库、HF 模型缓存卷、小说文本挂载、容器健康检查；
    容器内经 `host.docker.internal` 访问宿主机 Ollama
- **前后端类型契约**：`scripts/export_openapi.py` 导出 OpenAPI schema，
  openapi-typescript 生成 `frontend/src/api-generated.ts`，`api.ts` 的 REST 类型
  全部改为生成类型的别名导出（SSE 事件协议不在 OpenAPI 内，保留手写并注明原因）；
  CI 增加 drift 检查——改了 Pydantic 模型没重新生成会直接标红。
- **M3.5 第二阶段：忠实度 Judge 校准**：
  - 标注集 18 → 53 条（新增否定关系/跨句证据/多跳推理/程度篡改/代词链五类，
    partial 类加密），每条带标注理由
  - 影子脚本支持多 Judge 对比（`--model` 可重复传入）：跨方法汇总表 + 分歧样本
  - 三方实测（[校准报告](docs/experiments/m35-faithfulness-calibration.md)）：
    一致率 haiku 67.9% / flash 66.0% / glm-4.5-air 62.3%，全部未达启用门槛；
    所有 Judge 对「半真半假」系统性失明——已定义两档启用阈值，达标前保持影子调用

## [0.6.0] - 2026-08-22

### 新增

- **M3.5 引用忠实度与运行追踪（第一阶段，影子评测）**：
  - 引用质量三分类：正确性（编号有效）、完整性（非豁免事实陈述缺引用占比）、
    忠实度（`judge_support` 影子接口，绝不进入问答主链路）
  - 18 条人工标注的陈述—证据集（证据全部取自原创语料）+ `eval_faithfulness_shadow.py`
    对比脚本；首跑结果：规则基线一致率 16.7%，glm-4-flash Judge 77.8%，
    误判集中在「半真半假」细节反转类
  - 落库新增 `run_config` 快照：reranker/生成模型、回答模式与路由、prompt 版本、
    最终状态（新增 error 状态）；隐私红线有测试断言（不含 Key 与原文片段）
  - request_id / run_id / trace_id 职责边界文档化；Agent Lab 步骤补 run_id
- **M3.4 检索实验与失败策略（第一阶段）**：
  - `scripts/eval_matrix.py` 评测矩阵：每个配置在独立临时数据库从零建索引，
    记录配置指纹、Recall@K、MRR、索引耗时、查询延迟与存储成本，正式索引全程只读
  - Contextual Retrieval 三档模式 `CONTEXTUAL_MODE=off/auto/on`（默认 auto）：
    小体量书默认后台构建、大部头跳过，auto 档还检查生成后端可用性
  - 低置信度信号（`src/confidence.py`）：重排归一化分差、问题词覆盖率、跨书分散度，
    配套离线校准脚本 `scripts/eval_low_confidence.py`
  - 自适应查询扩展（`QUERY_EXPAND_ENABLED`，默认关闭）：信号不足时生成最多 3 个
    改写变体补救检索一次，触发原因/变体/耗时写入检索 trace
  - 整章扩展对照项（`CHAPTER_EXPANSION_MODE=chapter`）：命中片段所在整章按原文顺序
    进入 prompt，受真实 tokenizer 的 token 预算闸门约束，截断方向与范围写入 trace
  - 第一轮实验报告：[docs/experiments/m34-retrieval-matrix.md](docs/experiments/m34-retrieval-matrix.md)——
    chunk300 使 recall@1 从 0.8 降到 0.6；chunk800 被 M3.3 门禁正确拦截；
    BGE-M3 dense 在小语料无质量收益且成本更高（延迟 ×2.6、存储 ×1.4）。
    大部头验证（《凡人修仙传》全本）：chunk300 结论迁移成立（recall@1 -12.5pp）、
    BGE-M3 无收益结论强化（索引 ×11、延迟 +47%）；新发现大部头检索已达秒级延迟
- **夜间检索评测门禁（M3.3.5 收官）**：CI 每晚用仓库自带的原创小语料在临时数据库
  重建索引，跑检索评测并对照基线，核心指标（recall@1/3/5、MRR、路由准确率）回退
  超过容差即标红；`eval_retrieval.py` 新增 `--test-set/--strict/--tolerance` 参数，
  改检索链路的 PR 可手动触发提前验证。
- **索引任务状态落库（M3.3.5）**：任务卡片状态写入 PostgreSQL `index_task_runs`
  表，后端重启后侧栏能恢复上一次任务的最后已知状态；上次进程遗留的进行中任务
  会被如实标记为 failed 并提示可安全重试，不再凭空消失。
- 上传接口增加单文件大小上限（默认 20MB，可用 `MAX_UPLOAD_BYTES` 覆盖）：按 1MB
  一块流式读取，超限立即返回 413 `file_too_large`，不再把整个文件一次性读进内存。

### 优化

- **拆分 `src/rag.py`（1178 → 530 行）**：书名识别/意图、片段模型、召回方法、生成
  与 prompt 分别独立成模块，`rag.py` 保留编排层；对外 import 路径零改动。
- **前端 `App.tsx` 抽取自定义 hooks（820 → 391 行）**：聊天流与书架逻辑分别移入
  `useChatStream` / `useBookshelf`，组件树保持不变。
- 清理迁移后不再使用的 `chroma_db/` 遗留数据目录与 `.DS_Store`；`.gitignore`
  补充 `logs/`。

## [0.5.0] - 2026-08-22

### 新增

- **索引质量门禁与数据血缘（M3.3）**：入库前用真实 embedding tokenizer 检查最终输入的
  token 长度，报告编码方式、空/重复片段、章节覆盖和乱码提示；空输入、静默截断、异常
  向量或维度不一致会硬性阻止当前书替换旧索引。质量摘要随 `index_manifest` 保存，
  不含小说原文；`python scripts/check_index_quality.py` 可在写库前做质量预检。

### 修复

- 「我的书架」等处的藏书数量统计改为依据完整元数据计算，避免部分来源被漏计。

### 文档

- 全项目中文学习注释补全：为 backend（任务管理/中间件/模型客户端/.env 加载等 8 个
  文件）、src（路由/改写/引用评测/入库等 8 个文件）和前端（侧栏/SSE 解析/消息渲染等
  6 个文件）补充约 220 行解释设计意图的注释，不改变任何代码逻辑。
- 路线图新增 M3.3.5 工程加固里程碑；M3.4 改为 Contextual Retrieval 自动成本分级与
  BGE-M3 递进对照矩阵；M6 增加 ToolResult Schema 前置与只读 MCP PoC 探针。
- 同步过时文档：CHANGELOG 补记、README 学习路线表补齐链接、code-walkthrough 修正
  rag.py 行数并补充质量门禁导读、TEST_REPORT 标注为历史快照。

## [0.4.0] - 2026-08-10

### Added

- **Agent Lab 的对话现在也会保存了**：之前只有标准 RAG 模式支持刷新页面后恢复
  历史，Agent Lab 里问的每一次都是纯内存，刷新就清空。现在带上会话 ID 时，
  Agent Lab 的搜索步骤、观察记录和最终答案会一起落库，刷新页面能完整恢复出
  当时的步骤卡片，跟标准模式体验一致。

### Fixed

- Agent Lab 问到打错字的书名时（比如把"诡秘之主"打成"闺蜜之主"）不再直接报错——
  之前只有普通问答支持这种纠错，Agent Lab 里工具调用会因为书名没对上而失败。
- Agent Lab 反复卡在同一个失败工具上时，现在会更快察觉并换个方式继续，
  不会把几步的预算都浪费在同一个注定失败的调用上（只是换了个无关参数重试）。

## [0.3.0] - 2026-08-09

### Added

- 同一个输入框现在能自动区分开放问答和小说原文问题，也可手动指定回答模式；
  原文回答中的 `[n]` 引用可以直接定位并高亮带有书名、章节名和片段编号的出处，
  不再只能看到无法核对上下文的零散片段。
- 上传、修改或删除小说时改为**后台增量索引**：只处理发生变化的书，侧栏实时显示
  切分、Embedding、BM25 和入库进度，也能安全停止或查看失败原因后重试。每本书的
  向量和关键词索引原子切换，失败不会再清空全库或留下半套数据。
- 增加 **M3 层级检索**：为每本书建立章节摘要和全书摘要，主题、人物成长与跨书比较
  会先定位相关章节，再回到原文参与融合、重排和引用；旧索引只补摘要，不重算基础片段。
- 增加“📊 检索评测”面板：可逐层查看向量、BM25、RRF、重排的候选名次、分数变化
  和耗时，能判断正确片段是在召回、融合还是精排阶段丢失。
- 增加独立 **Agent Lab**：提供五个只读工具和最多 3～5 步的普通 Python 工具循环，
  界面实时展示选择理由、观察和证据 ID；无证据时拒答，不引入 LangGraph 依赖。

## [0.2.0] - 2026-08-09

### Added

- **等检索的那几秒不再是一片空白**：思考过程的步骤现在边跑边出，第一步 0.02 秒
  就到（以前要等检索全部跑完约 4 秒才一次性弹出）。每一步还会显示自己花了多久，
  一眼能看出时间花在哪（通常是精排那一步）。

### Fixed

- **回答流式输出时视图会一直跟着最新内容走了**。以前只要某一瞬间内容长得比较多
  （出处卡片一次性渲染出来、markdown 重新排版），自动跟随就会**永久卡住**，
  明明没碰过滚轮，却只能眼看着答案往下跑。现在只有你自己往上翻才会停止跟随，
  滚回底部或点「有新回复」就恢复。
- 思考过程里"合并去重后共 39 个候选"下一行却写"对 20 个候选重新打分"——
  少掉的 19 个去哪了没有交代。现在会写明"按 RRF 分数取前 20 个进入精排"。
- 答案开始输出后，思考过程面板会自动收起（手动展开过就不再自动收），
  收起时标题显示"1.6s · 5 步"而不是干巴巴的"5 步"。
- 侧栏"每次参考多少段原文"的默认值从 5 改成 3，与实测结论和后端默认值对齐——
  开了重排之后 3/5/10 三档命中率完全相同，取 3 能少送 39% 的字。

- 新增**实验性**的人物关系图（`GRAPH_ENABLED=1` 开启，**默认关闭**）。
  "某某有哪些伴侣"这类问题的答案散落在全书几十处，普通检索一次只能捞到一两处；
  关系图会在建索引时把人物关系整理成一张表，问到时直接查。
  ⚠️ **实测精度不够，暂不建议开启**：关系是靠"两个人名出现在同一段含关系词的
  文字里"推断的，会把师父、路人也算成伴侣。查到的结果标注了"统计推断"并交给
  AI 结合原文判断，所以不会产生错误答案——但也没能给出正确答案。
  保留这部分代码主要是作为学习材料，详见 docs/rag-techniques.md 第 6 节。
- **追问能听懂"他""那个"了**：以前问完"韩立的师父是谁"，再追问"他后来怎么样了"
  必然答不上来——因为搜索时只看这七个字，不知道"他"指谁。现在会先结合上文把问题
  补全成"李化元后来怎么样了"再去搜，补全结果也会显示在「思考过程」里。
  只在真的像追问时才触发（第一轮、以及本来就说清楚了的问题不会多花时间）。
- **停止生成**：回答生成中，发送键会变成「停止」。点了之后立即停住并保留已生成的内容，
  标记为「已停止生成」——不会显示成报错。更重要的是后端会真的停止向模型索取内容，
  用云端模型时不会在你按了停止之后继续消耗额度。
- **对话历史**：刷新页面或重开浏览器后，之前的问答、原文出处、思考过程都会自动恢复，
  被中途停止的那轮也会如实标出来。
- **有新回复提示**：往上翻看历史或原文出处时，如果下面来了新的回答，会出现「有新回复」
  提示，点一下就跳到底部——不用担心翻着翻着错过新内容。

### Changed

- 每次回答送给模型的原文片段从 5 段减到 3 段。这不是砍功能——实测（13 条用例）
  3 段和 5 段、10 段的命中率完全一样（都是 0.846），但 3 段少用 39% 的篇幅，
  相关内容占比还更高。少送无关内容能降低模型被带偏、答非所问的概率。
- 问到很短的书（全文 8000 字以内）时直接把整本给模型、不做检索，不会有任何
  信息遗漏。当前书架里只有《雾隐山庄》属于这一类；主要是为"上传一份小文档
  来问"这种用法准备的。
- 新增可选的「上下文增强」（默认关闭，`CONTEXTUAL_ENABLED=1` 开启）：小说里
  经常整段都是「他」「那汉子」这种代称、看不出在讲谁，这类片段搜不到。开启后
  会在入库时用 AI 给它们补一句说明（"某某和某某在哪里做了什么"）再建索引，
  搜的时候就能命中了。实测《降龙》里问"王慎在铁匠铺遇到谁在骚扰邻居"，
  不开时完全搜不到、开了排第 5。
  ⚠️ 代价是重建索引会明显变慢（每个片段要调一次 AI），所以默认只处理片段数
  较少的书、且只处理真正缺上下文的那部分（实测约占 35%）；生成结果会缓存，
  重复重建不会重复花时间。
- 检索多了一道「精排」工序：先粗筛出较多候选，再用一个更精细（但更慢）的模型
  从中挑出最相关的几段送给大模型。首次使用会自动下载约 1.1GB 的重排模型；
  下载不了或想关掉时设 `RERANK_ENABLED=0` 即可，问答功能不受影响。
- 后端 9 个接口补上了完整的响应模型声明，`/docs`（Swagger UI）和 `openapi.json`
  现在能看到每个接口真实的返回字段和类型，不再是空的占位符——如果你直接调用
  API 而不是走界面，现在能从 OpenAPI 契约里查到准确的返回形状。对界面使用者
  没有任何影响，返回的数据完全不变。
- 后端把散落的 `print()` 换成了正规的 `logging`，每次请求还会带一个
  request-id（响应头 `X-Request-ID`，也会打进这次请求相关的每一条日志），
  方便排查问题时把一次请求的完整过程串起来看。同样对界面使用者没有影响。
- 后端加了 PostgreSQL 连接池，一次问答请求不再每一步检索都重新握手连数据库。
  本机使用感觉不出差别，但去掉了"完全没考虑并发扩展"这个隐患。数据库暂时
  连不上时依然能正常启动（退化为逐次新建连接），不影响书架管理等功能。
- 后端错误响应统一成一个形状（带机器可读的错误类型），不再是每个接口各写
  各的自由文本。界面上看到的错误提示文案不变，但如果你直接调用 API，
  现在能按错误类型区分处理，而不是只能拿到一整段中文报错文字。

### Fixed

- 修了「精排」和「上下文增强」互相抵消的问题：上下文增强给片段补的说明只进了
  索引，精排却拿原文重新打分、看不到那些说明，于是把本来找对了的片段又压了
  下去。实测同一个片段，精排给原文打 0.0055、给「说明+原文」打 0.9990。
  修好后 recall@1 从 0.615 升到 0.692。
- **搜书里的细节时更容易找到了**。关键词检索之前有两个问题：一是只拿整句问题去
  原文里做子串匹配（"韩立有哪些伴侣"这句话不可能逐字出现在小说里，等于白搜）；
  二是就算匹配上了，也是"按片段在书里的先后顺序取前 20 个"、完全没有相关性排序
  ——实测《凡人修仙传》后半本书对关键词检索**完全不可见**。
  现在换成了 BM25：先把问题拆成词，再按"越罕见的词命中越值钱"打分排序。
  实测例子：问"韩立的名字是谁给取的、用什么换来的"，答案原文里的「窝头」全书
  只出现 1 次，改造前排在第 19 名（等于找不到），现在排第 1。检索还顺带快了约 3.6 倍。
- 用 Claude 订阅（`claude:` 开头的模型）回答时点「停止」，之前只是关闭了读管道，
  子进程如果当时没在写输出就不会立刻退出，可能继续跑到自然结束、白白消耗你的
  Claude 额度。现在会主动终止这个子进程，和用本地 Ollama、智谱 GLM 时一样，
  点停止就是真的停止。

## [0.1.0] - 2026-08-02

首个正式版本：本地小说 RAG 问答 + 三档生成模型（本地 Ollama / Claude 订阅 / 智谱 GLM），
检索思考过程可视化，可部署为公开仓库。

### Added

- **思考过程面板**：提问后可以看到检索是怎么一步步做的——识别到你在问哪本书（含错字纠正）、
  检索范围、三路召回各命中多少、结构性问题定位到书的哪个位置、最终取了几段作依据。
  折叠面板展示，都是真实数据，方便理解「为什么给出这个答案」。
- **跳到最近回答**：翻看历史消息或原文出处、离底部较远时，聊天区出现浮动按钮，
  一键滚回最新一轮回答。
- CI：push / PR 时自动检查前端类型和后端能否正常启动；`main` 分支已加保护，
  改动需经 PR 并通过 CI 才能合并。

### Changed

- 思考过程的生成中提示与折叠面板合并为一体，不再同时出现两个「进行中」的指示。

### 2026-08-01（错字容错、启动加速、发布准备）

- `新增` 书名错字容错（`rag.py: _fuzzy_contains`）：用户常打同音错字，例如把"诡秘之主"
  打成"闺蜜之主"（guǐ mì / guī mì）。精确子串匹配失败时改按编辑距离匹配（3 字标题容 1 字、
  4 字及以上容 2 字），并新增 17 条用例覆盖"不能误伤另一本书"的负例。
- `优化` 点明书名时把三路召回都限定在该书内（`rag.py: _named_novels`）：原先书名只约束
  结构性召回，语义/关键词仍全库搜，导致 5 个出处里 3 个是另一本书的无关片段、把模型带偏。
- `修复` 启动白等 20+ 秒（`src/embedder.py`）：`SentenceTransformer(name)` 每次都先访问
  huggingface.co 校验版本，网络不通时按 1+2+4+8+8 秒退避重试，**即使模型已完整缓存在本地**，
  还会打出误导性的 "Creating a new one with mean pooling"。改为优先 `local_files_only`
  加载、缺失才联网下载。后端就绪 **20s → 5s**，网络重试日志归零。
- `修复` **测试结果文件里嵌了 15 万字版权原文**（`tests/run_qa_tests.py`）：脚本把每段来源的
  完整正文写进结果 JSON 并已提交入库（`results_3b.json` 448KB）。改为只落盘 80 字摘录 +
  `novel`/`chunk_id` 定位信息，并清理了已入库的原文（448KB → 31KB）。
- `文档` 新增 `LICENSE`（MIT）与 README「版权与许可」章节，明确小说文本不随仓库分发。
- 检索耗时实测：三路召回 + RRF 融合合计 149–328ms（31449 个片段），非瓶颈。


### 2026-08-01（检索质量）

- `修复` **切分失效导致大量原文检索不到**（`src/loader.py`）：分段只按空行（`\n\n`）切，
  但小说站的 txt 普遍用**单换行**分段（《凡人修仙传》14 万个 `\n` 只对应 2468 个空行），
  于是切出 2468 个平均 **3108 字**的巨块，是 `CHUNK_SIZE=500` 的 6 倍。
  而 `bge-small-zh` 上限 512 token，**每个片段只有开头约 500 字进了向量，后面 84% 的内容
  在检索中完全不可见**。改为按任意换行切分 + 超长段落按句末标点二次切分，并硬保证
  不超过 `CHUNK_SIZE`。片段数 2468 → **19501**，平均 463 字，超限 0。
- `新增` 结构性位置检索（`rag.py: positional_retrieve`）：「结局/最后/开头」这类问题，
  答案原文里并不含这些词，语义检索几乎必然失败。改为直接按 `chunk_id` 取首/尾片段，
  与语义、关键词三路一起参与 RRF 融合。
- `新增` 正文结局锚点（`rag.py: _find_ending_anchor`）：网上的 txt 多为「全本+番外」，
  文件最末是番外和作者后记而非结局（《凡人修仙传》正文结局在 #19472「全书完」，
  之后 28 个片段全是番外）。改为优先定位「大结局/全书完/尾声」标记再向前取。
- `新增` 按人物名定位书籍（`rag.py: _dominant_novels`）：「韩立的结局」只提人物不提书名，
  原先会把每本书的结尾都拉进来。改为先用语义/关键词召回推断问题属于哪本书，再取其结局。
- 效果：「韩立的结局是什么样的」从「无法确定」变为答出「渡过天劫、飞升仙界」并附原文引用。

### 2026-08-01

- `新增` `.env` 自动加载（`backend/dotenv_lite.py`，零依赖）：密钥写在项目根 `.env` 即可，后端启动时自动注入，不必每次 `export`；`.env` 被 `.gitignore` 忽略，另提供可提交的 `.env.example` 模板。优先级为「真实环境变量 > `.env`」，便于临时覆盖。
- `新增` 启动日志打印已加载的 `.env` 变量名与云端可选模型列表（**只打变量名不打值**，避免密钥进日志），方便一眼确认 key 是否生效。
- `修复` GLM 选项从界面消失：根因是后端进程启动时环境里没有 `ZHIPU_API_KEY`，`zhipu.is_available()` 返回 False 导致 `/api/models` 不返回 GLM 四档。改用 `.env` 持久化后不再复现。

### 2026-07-31

- `新增` 接入智谱 GLM 作为云端生成模型（`backend/zhipu.py`）：`glm-4-flash`/`glm-4.5-air`/`glm-4.5`/`glm-4.6` 四档，Key 只从环境变量 `ZHIPU_API_KEY` 读取，不落盘；`/api/ask` 按 `glm:` 前缀路由；前端下拉框新增「☁️ 智谱 GLM」分组，云端隐私提示按厂商动态显示。
- `新增` 流式回答的打字机效果（`frontend/src/App.tsx`）：后端出字粒度不一（Ollama 逐 token / GLM 常整段返回），统一在前端排队按字匀速吐出。
- `新增` 「正在翻书思考…」思考指示（`MessageBubble.tsx` + `styles.css`）：正文到达前（模型推理/检索中）显示三点跳动动画，替代原来只有光标闪烁的空气泡。
- `优化` 界面视觉全面改版：从 antd 默认亮蓝改为「暖纸底 + 墨蓝主色 + 鎏金点缀」的书卷气主题；软化边框、加柔和阴影与悬停/聚焦微动效；出处卡片去掉刺眼蓝边、序号改鎏金描边、书名用宋体；同时写入 antd 主题令牌，浅色/深色一致。
- `修复` 打字机导致的长回答卡顿：① 帧率 60fps→30fps；② `MessageBubble` 整体 `React.memo`、出处卡片拆出独立 `memo`，流式期间旧气泡与省略号测量组件不再重渲染；③ 自动滚动改为「贴底才跟随 + rAF 合帧」。实测持续输出 90s+ 期间主线程长任务（>50ms）为 0。
- `文档` README 增补 GLM 配置说明与环境变量；`.gitignore` 忽略 `.env`；新增本文件与性能自查清单（`tests/PERF_CHECK.md`）。

<!--
写新条目时：
- 动词开头、一句话说清「改了什么 + 影响」，附关键文件路径。
- 归到当天日期下；分类用 新增/修复/优化/文档。
- 相对时间（如"今天""下周"）一律换算成绝对日期再写。
-->

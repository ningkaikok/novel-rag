# 安全说明

## 报告安全问题

如果你发现了安全问题（密钥泄露、注入、越权等），**请不要开公开 issue**，
直接邮件联系仓库所有者（GitHub 主页有联系方式），我会尽快处理。

## 密钥与敏感信息

这个项目会用到云端模型的密钥（如 `ZHIPU_API_KEY`），请遵守：

- **密钥只放项目根目录的 `.env`**，该文件已被 `.gitignore` 忽略，不会进版本库。
  仓库里只提供不含真实值的 `.env.example` 模板。
- **绝不要把密钥写进代码、提交信息、issue、PR 或截图**。后端只从环境变量/`.env`
  读取密钥，启动日志也只打变量名、不打值。
- 提交前用 `git status` 确认没有 `.env` 或任何含密钥的文件在待提交列表里；
  真要强制添加被忽略的文件（`git add -f`）前务必想清楚。
- **一旦密钥泄露（哪怕只是贴进了聊天），立即到对应平台吊销并重建**，
  例如智谱在 https://open.bigmodel.cn/usercenter/apikeys 。
  git 历史里的密钥即使后续删除也已算泄露，必须轮换。

## 本地/云端边界

- 默认路径（本地 Ollama + 本地检索）不向外发送任何数据。
- 切换到云端模型（Claude 订阅 / 智谱 GLM）时，**检索到的原文片段和你的问题会发送到
  对应厂商的服务器**，并计入你自己的账号用量。界面上有明确提示。自行评估是否上传敏感内容。
